<!DOCTYPE html>
<html lang="en" data-theme="dim">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Project</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mx-auto p-6">
    <h1 class="text-5xl font-bold text-center">Edit Project: <%= project.title %></h1>
    
    <form method="POST" action="/solutions/editProject" class="max-w-lg mx-auto mt-6">
      <input type="hidden" name="id" value="<%= project.id %>">
      
      <div class="mb-4">
        <label class="block mb-2">Title</label>
        <input type="text" name="title" value="<%= project.title %>" class="input w-full" required>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Feature Image URL</label>
        <input type="url" name="feature_img_url" value="<%= project.feature_img_url %>" class="input w-full" required>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Sector</label>
        <select name="sector_id" class="input w-full" required>
          <option value="1" <%= (project.sector_id == 1) ? "selected" : "" %>>Land Sinks</option>
          <option value="2" <%= (project.sector_id == 2) ? "selected" : "" %>>Industry</option>
          <option value="3" <%= (project.sector_id == 3) ? "selected" : "" %>>Transportation</option>
          <option value="4" <%= (project.sector_id == 4) ? "selected" : "" %>>Electricity</option>
          <option value="5" <%= (project.sector_id == 5) ? "selected" : "" %>>Food, Agriculture, and Land Use</option>
        </select>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Introduction (Short)</label>
        <textarea name="intro_short" class="input w-full" rows="4" required><%= project.intro_short %></textarea>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Summary (Short)</label>
        <textarea name="summary_short" class="input w-full" rows="4" required><%= project.summary_short %></textarea>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Impact</label>
        <textarea name="impact" class="input w-full" rows="4" required><%= project.impact %></textarea>
      </div>
      
      <div class="mb-4">
        <label class="block mb-2">Original Source URL</label>
        <input type="url" name="original_source_url" value="<%= project.original_source_url %>" class="input w-full" required>
      </div>
      
      <div class="flex gap-4">
        <button type="submit" class="btn btn-primary flex-1">Update Project</button>
        <a href="/solutions/deleteProject/<%= project.id %>" class="btn btn-error flex-1">Delete Project</a>
      </div>
    </form>
  </div>
</body>
</html>